'use strict';

angular.module('yaMaps')

/**
 * @ngdoc object
 * @name yaMaps.objects:InstanceTypes
 * @module yaMaps
 * @kind constant
 *
 * @description
 * Resource definitions for cloud instance.
 *
 * - `disk` - `{Number}` - max available disk space in GB
 * - `cores` - `{Number}` - max available CPU cores
 * - `ram` - `{Number}` - max available RAM
 */

;
